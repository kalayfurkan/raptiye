<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/icons/logo.svg">
    <title>İlan Formu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/default.css">
    <link rel="stylesheet" href="/css/forms.css">

	<meta http-equiv="Content-Security-Policy" content="
        default-src 'self';
        script-src 'self' 'unsafe-inline' https://code.jquery.com https://cdn.jsdelivr.net https://stackpath.bootstrapcdn.com;
        style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com;
        img-src 'self' data:;
        font-src 'self' https://fonts.gstatic.com;
        connect-src 'self';
        object-src 'none';
        form-action 'self';
        upgrade-insecure-requests;
    ">
</head>

<body>
    <div id="wrapper">
        <%- include('partials/navbar') %>
            <div class="container">
                <div class="form-container p-4 shadow rounded bg-white">
                    <h2 class="form-title text-center mb-4">İş İlanı Ver</h2>
                    <form method="post" action="/addjob" enctype="multipart/form-data" class="prevent-multiple-submit-form"> <!--id="ilanForm"-->
                        <div class="form-group mb-3">
                            <label for="ilanBasligi" class="form-label">İlan Başlığı</label>
                            <input name="jobTitle" type="text" class="form-control" id="ilanBasligi"
                                placeholder="İlan başlığını giriniz" required>
                        </div>
                        <div class="form-group mb-3">
                            <label for="sonTarih" class="form-label">Şu tarihe kadar ilanda kalacak</label>
                            <input name="removalDate" type="date" class="form-control" id="sonTarih" required>
                        </div>
                        <div class="form-group mb-3">
                            <label for="aciklama" class="form-label">Tüm Detaylar</label>
                            <textarea name="description" class="form-control" id="aciklama" rows="3"
                                placeholder="İlan açıklamasını giriniz" required></textarea>
                        </div>
                        <div class="form-group mb-4">
                            <label for="dosya" class="form-label">Görsel Yükle</label>
                            <small>(Bu kısım zorunlu değildir)</small>
                            <input name="images" type="file" accept=".avif, .webp, .png, .jpg, .jpeg" class="form-control" id="dosya" multiple>
                        </div>
                        <button type="submit" class="btn prevent-multiple-submit-button">İlanı Ver</button> <!--id="submitButton"-->
                    </form>
                </div>

                <div class="register-div">
                    <a href="/isilanlari">Tüm iş ilanlarını görüntüleyin</a>
                </div>
            </div>

            <%- include('partials/footer') %>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>