<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>İlanlar Listesi</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
	<%- include('partials/navbar') %>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Tüm İlanlar</h1>

    <div class="row">
      <% ilanlar.forEach(function(ilan) { %>
        <div class="col-md-4 mb-4">
          <div class="card">
            <img src="<%= ilan.images %>" class="card-img-top" alt="İlan Fotoğrafı">
            <div class="card-body">
              <h5 class="card-title"><%= ilan.title %></h5>
              <p class="card-text"><%= ilan.description %></p>
              <p><strong>İlan Türü:</strong> <%= ilan.searchingFor %></p>
              <p><strong>İletişim:</strong> <%= ilan.reachYou %></p>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  <%- include('partials/footer') %>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
