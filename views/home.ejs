<!DOCTYPE html>
<html lang="tr">

<head>
	<meta charset="UTF-8">
	<title>İTÜ Raptiye</title>
	<link rel="icon" href="/img/icons/logo.svg">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	<link rel="stylesheet" href="/css/default.css">
	<link rel="stylesheet" href="/css/home.css">

	<meta property="og:title" content="İTÜ Raptiye">
	<meta property="og:description" content="İTÜ öğrencileri için satış ve ilan sitesi">
	<meta property="og:image" content="/img/icons/logo.svg">
	<!-- <meta property="og:url" content="https://www.link.com"> güncellenecek -->
	<meta property="og:type" content="website">

	<!-- <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' https://cdn.jsdelivr.net https://stackpath.bootstrapcdn.com; style-src 'self' https://cdn.jsdelivr.net 'unsafe-inline';"> -->
	<!-- Implement a CSP to prevent XSS attacks and other code injection vulnerabilities. -->


</head>

<body>
	<div id="wrapper">
        <%- include('partials/navbar') %>
		<div class="container">
			
			<div class="intro">
				<h1 class="font-nunito">Raptiye</h1>
			    <p class="font-nunito">İTÜ öğrencileri için satış ve ilan sitesi</p>
			</div>

			<div class="feature-container">
				<div class="feature">
					<div class="feature-box hexagon-bg">
						<!-- buraya şekil şukul ekleniyor -->
						<div class="feature-content">
							<h2>Satış İlanı Ver</h2>
							<p>0 veya 2.el satmak istediklerini İTÜ'lülerle buluştur.</p>
						</div>
					</div>

					<div class="image-slider">
						<img src="/img/anasayfa/1.png" class="slider-image active" alt="İkinci El Örnek Ürün 1">
						<img src="/img/anasayfa/2.png" class="slider-image" alt="İkinci El Örnek Ürün 2">
						<img src="/img/anasayfa/3.png" class="slider-image" alt="İkinci El Örnek Ürün 3">
						<img src="/img/anasayfa/4.png" class="slider-image" alt="İkinci El Örnek Ürün 4">

					</div>
				</div>
				<div class="feature">
					<div class="feature-box circle-bg">
						<div class="feature-content">
							<h2>Genel İlanlar</h2>
							<p>Sözlük mü arıyorsun, önlük mü? Genel ilanlar sekmesine dilediğin ilanı koy.</p>
						</div>
					</div>
				</div>
				<div class="feature">
					<div class="feature-box">
						<div class="feature-content">
							<h2>Harf Notu Hesapla</h2>
							<p>Matematik, Fizik, Kimya harf notunu herkesten önce öğren.</p>
						</div>
					</div>
				</div>
				<div class="feature">
					<div class="feature-box">
						<div class="feature-content">
							<h2>Kampüse Mesaj Ver</h2>
							<p>Bilgi mi vermek istiyorsun yoksa şikayet mi etmek istiyorsun? Tüm kampüse sesini duyur.</p>
						</div>
					</div>
				</div>
				<div class="feature">
					<div class="feature-box">
						<div class="feature-content">
							<h2>İş/Ev Arkadaşı İlanları</h2>
							<p>İlgili ilanları kampüse duyur.</p>
						</div>
					</div>
				</div>
			</div>
			
		
		</div>

		<%- include('partials/footer') %>
	</div>

	<!-- Bootstrap 5 JS Bundle includes Popper -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+F/KNpi6kK+1b+Y5FQgR4V1S3bzQ" crossorigin="anonymous"></script>


	<script defer>
		document.querySelectorAll('.feature').forEach((feature, index) => {
			feature.classList.add(index % 2 === 0 ? 'align-left' : 'align-right');

			const hue = index * 40; // Increment hue by 60 for each feature
			//feature.style.color = `hsl(${hue}, 40%, 40%)`;
		});


		const observer = new IntersectionObserver((entries) => {
			entries.forEach((entry) => {
				console.log(entry);
				if (entry.isIntersecting) {
					entry.target.classList.add('show');
				} else {
					entry.target.classList.remove('show');
				}
			});
		});

		const hiddenElements = document.querySelectorAll('.feature'); //effect e sahip olacak olan tagler
		hiddenElements.forEach((el) => observer.observe(el));

	</script>

	<!-- sliding images -->
	<script defer>
		document.addEventListener('DOMContentLoaded', () => {
			const images = document.querySelectorAll('.image-slider .slider-image');
			let currentIndex = 0;
			const totalImages = images.length;
			const intervalTime = 3000; // 3 seconds
	
			// Function to show the next image
			const showNextImage = () => {
				// Current image
				const currentImage = images[currentIndex];
				// Calculate next index
				const nextIndex = (currentIndex + 1) % totalImages;
				const nextImage = images[nextIndex];
	
				// Add 'previous' class to current image to slide it out
				currentImage.classList.add('previous');
	
				// Activate the next image
				nextImage.classList.add('active');
	
				// Remove 'active' and 'previous' classes after transition
				setTimeout(() => {
					currentImage.classList.remove('active', 'previous');
					currentIndex = nextIndex;
				}, 1000); // Duration matches CSS transition
			};
	
			// Start the slider
			setInterval(showNextImage, intervalTime);
		});
	</script>
	
	<!-- Include Intersection Observer polyfill for older browsers -->
	<script src="https://polyfill.io/v3/polyfill.min.js?features=IntersectionObserver"></script>

</body>

</html>