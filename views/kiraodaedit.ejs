<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ev Arkadaşı Arayanlar / Ev Arayanlar</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/default.css">

</head>

<body>
    <div id="wrapper">
        <%- include('partials/navbar') %>
        <div class="container mt-5">

            <h1 class="text-center mb-4">Ev Arkadaşı Arayanlar / Oda Arayanlar</h1>
            <form action="/kiraoda/edit/<%=kira._id  %>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="type" class="form-label">İlan Türü</label>
                    <select class="form-select" id="type" name="searchingFor" required>
                        <option value="Ev Arıyorum" <%=kira.searchingFor==="Ev Arıyorum" ? "selected" : "" %>>Ev Arıyorum</option>
                        <option value="Oda Arkadaşı Arıyorum" <%=kira.searchingFor==="Oda Arkadaşı Arıyorum" ? "selected" : "" %>>Oda Arkadaşı Arıyorum</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="title" class="form-label">Başlık</label>
                    <input type="text" value="<%=kira.title %>" class="form-control" id="title" name="title" placeholder="İlan başlığınızı girin" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Açıklama</label>
                    <textarea class="form-control" id="description" name="description" rows="4"
                        placeholder="İlanınız hakkında detaylı açıklama yazın"
                        required><%=kira.description %></textarea>
                </div>
                <div class="mb-3">
                    <label for="contact" class="form-label">İletişim Bilgileri</label>
                    <input type="text" class="form-control" value="<%=kira.reachYou %>" id="contact" name="reachYou"
                        placeholder="Telefon veya e-posta" required>
                </div>
                <div class="mb-3">
                    <label for="images" class="form-label">Fotoğraf Yükle</label>
                    <input type="file" class="form-control" id="images" name="images" accept="image/*" multiple>
                </div>
                <button type="submit" class="btn btn-primary">İlan Ver</button>
            </form>
            <h3>Mevcut Görselleri Düzenle</h3>
            <div class="image-gallery">
                <% kira.images.forEach((image,index)=> { %>
                    <div class="image-item">
                        <img src="<%= image %>" alt="Görsel" width="200">
                        <form action="/delete-kiraodaimage/<%=kira._id %>/<%=index%>" method="POST" style="display:inline;">
                            <input type="hidden" name="imagePath" value="<%= image %>">
                            <button type="submit">Sil</button>
                        </form>
                    </div>
                <% }); %>
            </div>
        </div>
        <%- include('partials/footer') %>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>